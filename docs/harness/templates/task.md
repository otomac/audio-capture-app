# T<NNN> — <タスク名>

> **状態:** 未着手 / 進行中 / 完了 — YYYY-MM-DD
> **台帳:** [docs/tasks/backlog.md](./backlog.md)

## 1. 目的

<何のためにこれをやるのか。1〜2 文。>

## 2. スコープ境界

**やること**
- <変更する内容>

**やらないこと（重要）**
- **<意図的に触らないもの。ここが膨張防止線になる>**

## 3. 決定事項

実装中に蒸し返さない。変更するときは本セクションを直してから。

| # | 決定 | 結論 |
|---|---|---|
| D1 | <選択が必要だった点> | **<確定した結論>** |

## 4. 仕様書への影響

<該当する章と、何をどう直すか。影響が無いなら「仕様非影響。理由: <内部リファクタリングのみ／テスト追加のみ など>」と明記する。>

- [ ] `docs/spec/01_requirements.md` — <更新内容>
- [ ] `docs/spec/02_architecture.md` — <更新内容>
- [ ] `docs/spec/03_class_diagram.md` — <更新内容>
- [ ] `docs/spec/04_sequence_diagram.md` — <更新内容>

## 5. アーキテクチャへの影響

<層構成・依存方向・スレッドモデルに触れるか。触れるなら ADR 番号を書く。触れないならその理由。>

- ADR: <要 / 不要（理由）> → `docs/adr/NNNN-<slug>.md`

## 6. 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `AudioCaptureApp/Services/<X>.cs` | <何をどう変えるか> |

## 7. 実装手順

上から順に実行する。グループごとにビルドを通す。

### グループ A — <テーマ>
- [ ] **A1** <命令形で 1 手順> (`path/File.cs`)
- [ ] **A2** …

### グループ Z — 検証（必須・最後に置く）
- [ ] **Z1** `dotnet build AudioCaptureApp.slnx -c Debug` — 警告 0 件
- [ ] **Z2** `dotnet format AudioCaptureApp.slnx --verify-no-changes` — 差分なし
- [ ] **Z3** `dotnet test AudioCaptureApp.slnx -c Debug` — 全件成功
- [ ] **Z4** 仕様書（§4）の更新反映を読み直す

## 8. テスト一覧

<テストプロジェクト `AudioCaptureApp.Tests` に追加・変更するもの。実機デバイス／Whisper モデルを要求するテストは書かない。>

- **`<TestMethodName>`** — <このテストが保証すること 1 行>

> **テストで守れない範囲:** <ユニットテストでは検証できない部分を正直に書く>

## 9. 未解決の質問

事実ではなく **判断** を要するもの。既定案を添えて確認する。

1. **<質問>** — 選択肢: <A / B>。*既定案: <A>。確認をお願いします。*

## 10. 前提

<この計画が成り立つために正しいと仮定していること。後から崩れうるもの。>

---

## 実行結果 (YYYY-MM-DD)

完了時に **実測値** を記入する。「通った」ではなく件数を書く。

- `dotnet build` : 警告 <n> 件 / エラー <n> 件
- `dotnet format`: 差分 <あり / なし>
- `dotnet test`  : <n> 件成功 / <n> 件失敗 / <n> 件スキップ
- 計画からの逸脱: <あれば書く。なければ「なし」>
