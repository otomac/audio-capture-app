# タスク台帳

**このファイルが唯一の進捗管理表である。** 様式は
[docs/harness/60-task-format.md](../harness/60-task-format.md) に従う。

- 状態: `[ ]` 未着手 / `[~]` 進行中 / `[x]` 完了 / `[-]` 取り下げ
- 同時に `[~]` にしてよいのは **原則 1 件**
- 行はセクション間を **移動** させる（複製しない）
- ID は再利用しない

---

## 進行中

- [~] **T154** **UI の改良（このタスクは単独で実施すること — 他のタスクと同じブランチに混ぜない）。** 目的は 4 つ。①基本レイアウトをコンパクトにする、②デザインをモダンにする、③常時表示が不要なものはダイアログへ移す、④**デザインの検討には人の判断が必要**（案を出して合意を得てから実装する）。**進め方:** 実装の前に現状と変更案を並べて利用者の承認を得る。③でウィンドウを増やす場合は [ADR-0002](../adr/0002-secondary-windows-share-mainviewmodel.md) の規則に従い、新しいウィンドウは `MainViewModel` を `DataContext` として共有する状態レスな View にする（別の ViewModel を作らない）。**T149 / T151 / T152 / T153 はいずれもメインウィンドウのレイアウトを触る**ため、**それらを先に片付けてから着手するほうが差分が小さくなる**（順序は利用者と相談）。要求仕様は §8 / §9 / §12 の UI 記述、ウィンドウを増やすなら `docs/spec/02_architecture.md` の View 一覧も対象。利用者の依頼 (2026-08-23) → [詳細](./T154-ui-refresh.md)

## 未着手

- [ ] **T148** 話者 ID の決定を自前に置き換える案（[ADR-0004](../adr/0004-speaker-id-assignment-strategy.md) 案 C）。sherpa-onnx からは**話者区間の時刻だけ**を受け取り、各区間の埋め込みを `SpeakerEmbeddingExtractor`（既存 API・依存の追加なし）で取り直して、**3 秒以上の区間だけ**で凝集型クラスタリングを行い、短い区間はできた重心へ寄せる。T146 の試作では **基準音声で使用 ID 6 → 2（実際の人数）を、正解 4 箇所の区別を 4/4 保ったまま**達成し、正解が全区間で分かる合成音声でも 3〜4 ID → 2 ID・正答率 92.0% → 92.4% / 97.5% → 97.5%、単独話者の録音 2 本は 1 人のまま、会議は使用 ID 72 → 20 / 36 → 13、処理時間の増加は約 1 割だった。**T115 以降で「ID の数」と「区別」が同時に良くなった初めての構成である。**それでも ADR-0004 で見送ったのは、**しきい値の安全域が実測 6 本で幅 0.12 しかなく**（類似度 0.53〜0.65）、外した側の壊れ方が「全員が 1 人にまとまる」という沈黙的な誤りだからである（実際 0.50 では合成音声が正答率 92% → 52% に潰れた）。**着手の条件は 3 つ**（ADR-0004「案 C を採用してよい条件」）: ①本物の多人数会議 1 本について参加人数と主要な交代の正解を用意する、②固定しきい値ではなく**併合類似度の段差**で切る設計にして実測で確かめる、③潰れたこと（使用 ID が 1 種になる等）を利用者に知らせる。**ADR-0004 は 2026-08-23 に承認済み**（決定は「今は現状維持、案 C は条件付きで本タスクへ送る」）。したがって残る関門は上の 3 条件であり、**①が揃うまで着手しない**。T146 の実測で発見
- [ ] **T144** 相槌・短い割り込みに対して話者区間が生成されない（①検出漏れ）ことへの打ち手が、現時点で存在しない。T142 で sherpa-onnx が配布する segmentation モデル 3 種すべて（pyannote-segmentation-3.0 / reverb-diarization-v1 / reverb-diarization-v2）を実測したが、**全部が同じ 2 か所を取り逃がす**。reverb 系は Rev Model Non-Production License で商用・本番利用が禁止されているため、精度が良かったとしても採用できない。**新しい segmentation モデルが sherpa-onnx に追加されたら再評価する**（それまで着手しない）。ライセンスが実用可能であること、処理時間が現行（音声長の約 0.12 倍）から大きく悪化しないことを採用条件とする。**再確認 (2026-08-23・T146 の作業中):** sherpa-onnx の `speaker-segmentation-models` リリースの資産は pyannote-segmentation-3.0 / reverb-diarization-v1 / reverb-diarization-v2 の 3 種のままで、**2024-12-15 以降 1 つも追加されていない**（本体は 1.13.6 まで出ている）。着手条件は満たされないため未着手のまま。あわせて T146 で「交代が短い（中央値 1.2 秒）音声では話者交代の 9% しか検出されず、正答率は当て推量と同じ 51.4%」ことを正解付きの合成音声で定量化した — 本件がどれだけ効いているかの実測値である。次回もこのリリース資産を確認すること。T142 の実測で発見
- [ ] **T134** ファイル文字起こしが失敗したとき、`Error` イベントで通知した**失敗理由がステータスバーに残らない**。ワーカーが `Error?.Invoke("ファイル文字起こしエラー: ...")` で `Dispatcher.BeginInvoke` を積んだ直後に、`await Task.Run(...)` の継続が同じ Dispatcher へNormal 優先度で積まれ、`StatusMessage = "文字起こしに失敗しました"` が上書きすると読める（同一優先度の FIFO）。ダイアログは REQ-TRX-FILE-12 で自動的に閉じるため、手がかりがステータスバーだけになり原因が分からない。T115 以前からある挙動だが、REQ-TRX-DIA-11 が「どのモデルで失敗したか分かるメッセージ」を要求したことで実害が顕在化した。**まず実機で再現させ、上書き順序が読みどおりか確認してから修正する**（コードリーディングのみの推測段階）。T115 のレビューで発見

## 完了

- [x] **T153** **Whisper の文字起こし言語を指定できるようにする**。現在は `TranscriptionService.cs:389 / 625 / 796` の 3 か所で `WithLanguage("ja")` が**ハードコードされている**。ライブ文字起こしとファイル文字起こしの**両方に対応し、かつ互いに独立して設定できる**ようにする。ライブ側はメインウィンドウのドロップダウン、ファイル側はオプション指定ダイアログ（REQ-TRX-FILE-09）のドロップダウン。**決めることが 3 つある:** ①選択肢をどこまで並べるか（Whisper は 99 言語。全部出すのか、日本語・英語などに絞るのか）、②「自動判定」を選べるようにするか（Whisper は言語検出ができるが**短いチャンクでは誤検出しやすく**、ライブ経路は 20 秒チャンクなので相性が悪い可能性がある。**実測してから決める**）、③設定の永続化（`AppSettings` にライブ用・ファイル用の 2 つを持つ）。要求仕様 §7（共通基盤）に言語の要件を追加し、§8 / §9 に UI を追記する。利用者の依頼 (2026-08-23) (2026-08-24) → [詳細](./T153-transcription-language.md)
- [x] **T152** **話者識別（ダイアライゼーション）が使える状態かをメインウィンドウで分かるようにする**。①起動時にステータスバーへ表示する、②「音声ファイルから文字起こし」ボタンの近くに**編集不可のチェックボックス**「話者識別」を置く。**着手前に押さえるべき事実:** 現在 `SpeakerDiarizationService` は**モデルの読み込みを初回実行まで遅らせている**（`MainViewModel.cs:77-87` のコメントと `SpeakerDiarizationService.EnsureLoaded`）。そうしているのはモデル未配置でも起動を妨げないためである（ADR-0003 N2 / REQ-TRX-DIA-08）。したがって**起動時点では「読み込めるか」は分からない**。取りうる案は (a) モデル 2 ファイルの存在検査だけを起動時に行う（軽い・確実だが「壊れたモデル」は検出できない）、(b) 起動時に実際に読み込む（確実だが起動が遅くなり、ADR-0003 N2 の判断を覆すため ADR が要る）、(c) 初回実行後にだけ状態を更新する。**(a) を既定案とする。** 表示は「有効／モデル未配置／無効」の 3 状態を区別できる文言にすること。要求仕様 §13 に要件 ID を追加。利用者の依頼 (2026-08-23) (2026-08-24) → [詳細](./T152-diarization-status.md)
- [x] **T151** メインウィンドウから**ファイル文字起こしの進捗表示（不定プログレスバー＋状況テキスト）を削除する**。`MainWindow.xaml:264-279` の「進捗表示」`StackPanel`（`IsTranscribingFile` で表示切替）が対象。オプション指定ダイアログ（REQ-TRX-FILE-09 / 11）が百分率つきの進捗を出すため二重になっている。**ただし REQ-TRX-FILE-13 が「処理中にダイアログを閉じても処理は継続し、メインウィンドウ側の進捗表示と『中止』ボタンが引き継ぐ」と定めている。**削除するとこの要件が成り立たなくなるため、**仕様を先に改訂する** — ダイアログを閉じたあと進捗を見えなくしてよいのか、「中止」ボタンだけ残すのか、ステータスバーに 1 行だけ残すのかを決める。要求仕様 §9 の REQ-TRX-FILE-06 / 13 の改訂が要る。利用者の依頼 (2026-08-23) (2026-08-23) → [詳細](./T151-dialog-only-progress.md)
- [x] **T150** ファイル文字起こしの**開始時刻（REQ-TRX-FILE-10）に既定値を自動で入れる**。現在は常に空欄で、利用者が毎回手で入力している。取得元の候補は 3 つあり、**上から順に試して最初に取れたものを採用する**案。①**ファイル名から**（本アプリが録音した `yyyyMMdd_HHmmss.mp3` 形式。最も確実）、②**ファイルの作成日時**（**最終更新日時ではない**。`File.GetCreationTime`）、③**最終更新日時 − 音声の長さ**（録音終了時刻から逆算する）。※依頼文の「テキストファイル内の文字起こし時刻範囲」は、同名の `.txt` / `.transcript.txt` があればその時刻範囲を使う案とも読めるため、**①②③のどれと組み合わせるのか着手前に要確認**。**注意すべき事実:** ②の作成日時はファイルをコピー・移動すると「コピーした日時」になるため当てにならないことがある。③は無音カット後ではなくファイル全長を使うこと。いずれも**推定値なので、ダイアログ上で「自動入力した」ことが分かるようにし、利用者がそのまま消せる**ようにする（空欄＝未指定は従来どおり有効）。UI は `FileTranscriptionOptionsWindow`、解析は `MainViewModel.TryParseStartTime` の隣に `internal static` の純粋関数として置けばテストしやすい。利用者の依頼 (2026-08-23) (2026-08-23) → [詳細](./T150-auto-start-time.md)
- [x] **T149** 録音中・文字起こし中にウィンドウの × ボタンで閉じようとしたとき、**確認ダイアログを出す**。録音中なら「録音中ですが終了しますか？」、YES なら通常の停止処理（`.mp3` の確定・`.txt` の追記）を通してから終了する。ファイル文字起こし中なら「文字起こし中ですが中止して終了しますか？」、YES なら中止処理（REQ-TRX-FILE-07 = 生成中の出力ファイルを削除）を通してから終了する。NO ならウィンドウを閉じない。現状は `MainWindow.xaml.cs:21` の `Closed += (_, _) => Dispose();` だけで、**`Closing` を捕まえていないため確認も中断もされない**（進行中の録音がどう畳まれるかは `Dispose` 任せ）。`Closing` で `e.Cancel` を使う形になるため、**非同期の停止処理をどう待つか**が設計上の争点になる（`Closing` は `await` できないので、「一度キャンセルし、停止完了後に自分で `Close()` を呼び直す」形が定石）。要求仕様は §2（録音制御）と §9（ファイル文字起こし）に要件 ID を追加する。利用者の依頼 (2026-08-23) (2026-08-23) → [詳細](./T149-close-confirmation.md)
- [x] **T145** 話者ダイアライゼーションの推論スレッド数の既定を 1 → **`min(4, 論理コア数)`** にした。**起票時に根拠とされていた「1 スレッド 88.4 秒 → 4 スレッド 20.1 秒（4.4 倍）」は再現しない** — 同じ音声・モデル・設定で測り直すと **28.3 秒 → 22.3 秒（1.27 倍）** である。88.4 秒の正体も分かった: **16 スレッド分の CPU 負荷を掛けながら測ると 1 スレッドで 85.0〜88.1 秒が再現する**。T138 の計測が並行実行中だったためと考えられる。したがって既定を上げる根拠は速さではなく**混雑時の落ち込みの小ささ**である（負荷 16 で 1 スレッド 85 秒 対 4 スレッド 47 秒）。上限を 4 にしたのは実測で **8 スレッド 31.0 秒 / 20 スレッド 38.4 秒**と悪化するため（15 分の音声でも 20 スレッドは 1 スレッドより遅い）。**結果はスレッド数で変わらない**ことを 1〜20 スレッドで再確認（話者区間 38 本が開始・終了・ID まで完全一致）。**録音スレッドとの競合は無い** — 10ms 周期スレッドの遅れは推論 1 / 4 スレッドで差が無く（p50 5.54ms・p99 6.3〜6.5ms）、負荷 16 でも変わらない。**既存の `settings.json` は書き換えない**ため、`"SpeakerDiarizationThreads": 1` が書かれている環境では利用者が手で直す必要がある（README に明記）。REQ-TRX-DIA-14 を新設 (2026-08-23) → [詳細](./T145-diarization-thread-default.md)
- [x] **T146** 話者 ID 断片化の**原因を直接測って確定させた**（T138 の推論を裏づけた）。話者埋め込みは切り出しが短いほど「同一人物を同一と認める力」を失う — 同一話者どうしのコサイン類似度は**0.25 秒で 0.31 / 1 秒で 0.51 / 3 秒で 0.76 / 10 秒で 0.90**、別話者どうしは長さによらず 0.22〜0.44。**既定の閾値 0.5 はちょうど「1 秒の区間の同一話者類似度」と同じ高さ**にあり、実際の区間は半数が 1 秒未満なので、**同一人物の区間の約半分が閾値を越えられない。** 0.25〜0.5 秒では両分布がほぼ完全に重なり、閾値をどこに置いても分けられない。さらに**正解が全区間で分かる合成音声**（単独話者の録音 2 本を交互につないだ 3 本）で測ると、**交代が短い（中央値 1.2 秒）音声では話者交代の 9% しか検出されず正答率 51.4%（＝当て推量）** — 断片化を直しても相槌には届かないことが分かった。**出力の後処理（短い区間の ID を隣へ寄せる）は費用に見合わない**（安全な範囲では会議の使用 ID が 72 → 70 しか減らない）。**自前で ID を付け直す試作は明確に良かったが、しきい値の安全域が狭いため今回は見送り**、[ADR-0004](../adr/0004-speaker-id-assignment-strategy.md) を起票して T148 へ送った。ソース変更なし。要求仕様 §13 と README に実測を明記 (2026-08-23) → [詳細](./T146-short-segment-embedding.md)
- [x] **T143** Whisper のトークン時刻がセグメント末尾へ押し込まれる件。**現象は実在するが、対策はいずれも成立しないと分かった。**起票時の対策案「飽和した末尾を次セグメントの開始まで伸ばす」は**原理的に効かない** — Whisper のセグメントは隙間なく連続して並ぶため、飽和している 20 セグメントのうち **17 件で伸ばす余地が 0 秒**である。余地を無視して 0.2〜1.0 秒伸ばすと**正しく取れていた区別を 1 つ失い**（4/4 → 3/4）、直る行は 0 件。発話時間帯に ±0.1〜0.5 秒の許容幅を持たせる変種も、直る行 0 件・誤り 1 件増（`#14` が隣の話者へ）。さらに**起票時の見立て（`#12`「へぇー」の誤りは末尾飽和が原因）自体が誤りだった** — 切り出し位置を変えて 12 通りにデコードし直すと「へぇー」は **12 回中 2 回しか現れず**、細かい時刻が出た 4 回はいずれも境目を 24.26 秒に置く（本体は 24.00-25.00 に置き、Diarization は 25.12-25.44 を検出）。**Whisper が重なった発話を直列のセグメント列へ置き直すときの時刻は当てにできない**という、後処理では届かない誤りである。**実音声の聴取は行っていない**が、どちらの時刻が正しくても対策は同じ（何もしない）ことを実測で確認した。ソース変更なし。要求仕様 §13 の「時刻の精度」注記を差し替え (2026-08-23) → [詳細](./T143-token-timestamp-saturation.md)
- [-] **T147** 参加人数をファイル文字起こしのオプションダイアログで指定する案。**実測の結果、取り下げた。****人数を渡すと結果は悪くなる。** 基準音声（2 名・正解の分かる 4 箇所）に正しい人数 2 を与えると**区別が 4/4 → 1/4** に落ち、`k` を 2〜6 のどこに振っても同じだった。さらに**正解が全区間で分かる合成音声**を作って測ると、`KnownSpeakerCount=2` は**話者を 1 つに潰し、正答率 92.0% → 52.0%（＝当て推量）**にした。T115 のレビューで報告されていた「相手の話者に飲み込まれる」現象を、初めて定量的に裏づけたことになる。**渡し方（設定か UI か）の問題ではなく、渡すという入力そのものが有害**なので UI を足しても解決しない。着手前の確認事項②（`OfflineSpeakerDiarization.SetConfig`）は**使えることを実機で確認した** — モデルを読み込み直さずに 0.00 秒で設定を差し替えられ、結果は新規生成と完全に一致する（将来ファイルごとに閾値を変えたくなったときの前提として記録）。ソース変更なし。要求仕様 §13 と README に実測を明記 (2026-08-23) → [詳細](./T147-known-speaker-count-in-dialog.md)
- [x] **T138** 同一人物の話者 ID が断片化する（A が `話者1`/`話者2`/`話者6`/`話者29`/`話者30` に割れた）。**音声 5 本 × 埋め込みモデル 4 種 × 閾値 0.4〜0.7 を再測し、既定値（campplus zh_en / 0.5）を変えないと決めた。** 評価軸を「使用話者 ID の数」だけでなく「正解が分かっている 4 箇所の区別を保てるか」の 2 軸にしたところ、**区別 4/4 を保てるのは campplus の 0.5 と 0.6 だけ**で、代替 3 種（eres2netv2 / TitaNet-large / WeSpeaker ResNet293）は閾値をどう振っても必ず「間違いなそう」を失った（T138 起票時に「有害」の指標として挙げられていた区別そのもの）。ResNet293 は 2 名の対話で ID 2 種という理想的な数を出すが、それは別人を飲み込んだ結果であり（0.6 以上では全 58 行が 1 人になる）、しかも 12 倍遅い。閾値は **0.4 まで下げると単独話者の録音が 2 人に割れ**、**0.7 まで上げると区別を 3 つ失う**ため、使えるのは 0.5 と 0.6 の 2 点だけで、その 2 点は正解の分かる材料で同点。**差が付かないので動かさない。** 断片化の真因は閾値でも埋め込みモデルでもなく**短い区間の埋め込みが安定しないこと**と判明（→ T146）。なお ResNet293 は会議 2 本で ID を 45〜108 種 → 7〜11 種まで減らす（実時間の 1.4〜1.7 倍）ため、「人数の見通しを優先したい」利用者向けの選択肢として README に記録した。ソース変更なし。要求仕様 §13 と README に制約を明記。計測中に T145 / T146 を起票 (2026-08-23) → [詳細](./T138-speaker-id-fragmentation.md)
- [x] **T135** 話者ダイアライゼーション有効時、`SegmentTranscribed` が**書き出しの最後に全行まとめてバースト発火**する（`WriteAttributedSegmentsAsync` のループ）。表示ウィンドウと同じ構成（`ListBox` ＋ `ObservableCollection` ＋ 後回しの `ScrollIntoView`）を再現して実測したところ、2,100 行で **捌け切るまで 665〜735ms／UI の詰まり最大 177ms**。**起票時に候補として挙がっていた「まとめて 1 回の発火」は効かない**と判明した（実測 667ms）— 費用は Dispatcher のキューではなく 1 行ごとの `CollectionChanged` → レイアウトだからである。採ったのは間引きで、`MainViewModel` が行をキューに溜めて 1 回で引き取り、**表示上限を超えるぶんは `ObservableCollection` へ追加せずに捨てる**（追加してから捨てても最終状態は同じであることをテストで固定）。実測 **143ms／最大 99ms**（100ms 超の詰まりが 0 回に）。ライブ経路は 1 回の発火につき 1 行しか溜まらないため挙動は不変。REQ-LIVEVIEW-09 を新設、テスト 5 件追加 (2026-08-23) → [詳細](./T135-live-transcript-burst.md)
- [-] **T141** 1 つの Whisper セグメントの中で話者が切り替わる場合に、テキストを分割できない（例: 「あマジで」＋別話者の発話が 1 行に同居）。**頻度を測った結果、当面は実装しないと決めた。** セグメント内に 0.3 秒以上の話者境界があるのは 5 本の音声で 0〜30.5%（0.5 秒以上なら 0〜21.3%、1.0 秒以上なら 0〜6.4%）だが、**この数字は上限にすぎない** — 話者 ID が 2 名の対話で 16 種・会議で 108 種に断片化しており（T138）、数えている境界の大半は別人への交代ではなく同一人物の断片どうしの境目である。実際、断片化が軽い音声ほど見かけの境界も減る（108 種の会議 30.5% → 45 種の会議 18.3%）。一方で実装コストは起票時の想定より重く、U+FFFD で壊れたトークンを含むセグメントは 5 本すべてで **37〜81%**（起票時は 18 件中 9 件）。分割が触るのはテキスト本体であり、再同期をしくじれば文字化けした行が出る。効いてほしかった混在 3 件に効かないことは T137／T142 で確定済み。**T138 が解決して話者 ID が実人数まで収束したら、境界 ≥0.5 秒を測り直して新 ID で再起票する**（T143 の時刻ずれも先に要解決） (2026-08-23) → [詳細](./T141-intra-segment-text-split.md)
- [-] **T139** 有声区間の分割位置（`SplitVoicedRegions`）に話者境界の情報を足す案。**実測の結果、起票時の前提「区間数を増やさず」が成り立たないため取り下げた。** 2 話者の対話では「交代点に切ってよい無音がある」区間が **0/9** で 1 度も発火しない。会議では 48 区間中 23 本（もう 1 本の会議は 41 区間中 17 本）で発火するが、**その区間はほぼ全部が 20 秒チャンクまるごと 1 本**である（発話密度が高いと `NoSplitVoicedRatio` 0.9 が働くため）。つまり現状の区間はすでに最大で、話者境界で切ることは「位置を選び直す」ことではなく **区間を増やすこと**にほかならず、T127（停止レイテンシ）と T112／T125（短断片のハルシネーション）が潰した問題を買い戻す。加えて切る位置の根拠にする話者境界そのものが信用できない（会議で 108 種／45 種の断片化。T138）。**音声を切る位置は間違えるとテキストを壊す** — 要求仕様 §4 と ADR-0003 争点 2 が Diarization での音声切り分けを禁じているのと同じ理由である。**T138 が解決したら、理由 1・2 の数字を測り直して新 ID で再起票する**（ただし「区間を増やしてよいか」から議論し直す必要がある） (2026-08-23) → [詳細](./T139-speaker-aware-region-split.md)
- [x] **T142** 話者の割り当てを誤る箇所の真因が Diarization 側にあると判明した（T137 の実測）。2 つある。**①検出漏れ** — 短い割り込み（`#8` 15:08:16「えー、どれまでですか?」）に対応する話者区間が 1 つも生成されず、`15:08:15.03-15:08:28.97 話者1` の一本に飲まれている。`MinDurationOn` を 0.3→0.2 にしても全 58 行のラベルは変わらなかった（T115 のレビューで実測済み）。**②同時発話** — 話者区間どうしが時間的に重なる場合（`#13` は話者1 13.9 秒に話者4 0.32 秒が重なる、`#50` は話者30 と話者8 が重なる）、「最大重複の話者を選ぶ」規則では長い方が必ず勝ち、短い割り込み側は原理的に選ばれない。REQ-TRX-DIA-05 の規則そのものが同時発話を想定していない。**実測の結果、対策はいずれも入れないことにした。** ①は真因だが打ち手が無い — `MinDurationOn` を 0.3→0.05 まで下げても問題の 2 か所には話者区間が 1 本も生まれず（総数は 38→51 本に増えるが別の時間帯）、代替 segmentation モデル 2 種（reverb v1/v2）も同じ箇所を同じように取り逃がす（加えて両方とも商用不可ライセンス、v2 は 9 倍遅く 2 分 42 秒を 6 区間にしか割らない）。**②は誤りの原因ではなかった** — 重なり自体は多い（58 件中 24 件）が、その大半は同一人物の断片どうしで、規則の変種 4 つを実データに当てても「包含優先」「排他重複」は 1 行も動かず、「僅差は話者不明」は正しかった 7 行を失い、「区間長で正規化」は使用話者 ID が 6→15 に増えて明確に悪化した。**代わりに起票時に挙げていなかった③ Whisper のトークン時刻のずれが浮上した**（末尾トークンが長さ 0 のセグメント 20/58 件、話者境界が発話時間帯の 0.5 秒外側にあるセグメント 28/58 件）→ T143。ソース変更なし、要求仕様 §13 に制約を明記 (2026-08-23) → [詳細](./T142-diarization-attribution-root-cause.md)
- [x] **T137** 話者の重複長を、Whisper セグメントの時間範囲ではなく**トークンが実際に存在する発話時間帯**の合計で測るようにした（REQ-TRX-DIA-13）。最小長パディングの無音や発話後の余白を重複に数えなくなる。`WithTokenTimestamps()` を Diarization 経路にのみ付け、DTW は使わない（モデルごとの alignment heads プリセットが要り、モデルパスが設定で差し替わる本アプリでは対応付けを保証できないため。実測で DTW なしでも十分）。テスト 20 件追加、既存 19 件は無改修で通過。**着手時の前提は外れた** — 起票時は「混在 2 件の原因はこれ」と判断していたが、実測では混在は 2 件のまま変わらず、真因は Diarization 側の検出漏れと同時発話だった（→ T142）。本タスクが実際に効いたのは断片化で、B 側が 3→2 に減った (2026-08-23) → [詳細](./T137-token-level-speaker-attribution.md)
- [x] **T140** 改行コードがリポジトリ内で混在している。**同じ種類のファイルで割れており規則性が無い** — `AudioCaptureApp.csproj` は CRLF だが `AudioCaptureApp.Tests.csproj` は LF、`README.md` は CRLF だが `docs/` 配下 56 件は LF、`AudioCaptureApp.Tests/.gitignore` は CRLF だがルートの `.gitignore` は LF。`.gitattributes` が無く `core.autocrlf=false` のため、どちらで書いても記録されてしまう。実際に T115 の作業中、正規化スクリプトが `docs/` を CRLF に変換し、**実質 2 行の変更が 224 行の差分**に化けてレビュー不能になった（`docs/spec/04_sequence_diagram.md` は 59 行 → 631 行）。amend で直したが、強制が無い以上いつでも再発する。`* text=auto` でリポジトリ内を LF に統一し、`.editorconfig` が `end_of_line = crlf` を要求して `dotnet format` が検証する `.cs` / `.xaml` だけ `eol=crlf` で作業ツリーへ展開する。**CRLF で記録されている約 35 ファイルが一度だけ全行書き換わる**（空白のみの差分で中身は不変）。T115 のレビューで発見。`* text=auto` ＋ `.cs`/`.xaml` の `eol=crlf` ＋ 画像の `binary` を `.gitattributes` に定義し、`git add --renormalize` で一括正規化した。36 ファイルが書き換わったが、`--ignore-all-space` を掛けると差分は `.gitattributes` と本台帳の起票行だけで、**中身の変更はゼロ**であることを確認済み。記録側は全ファイル CR=0、作業ツリーの `.cs` は CRLF 維持（G2 が通ることで裏付け） (2026-08-22)
- [x] **T136** `docs/spec/01_requirements.md` の REQ-TRX-DIA-11 が実装箇所を`TranscriptionService.TranscribeFileWithDiarizationAsync` としているが、実際に `Error` イベントを発火しているのは例外を捕捉する側の `TranscribeFileAsync` である（`TranscribeFileWithDiarizationAsync` は `SpeakerDiarizationException` を投げるだけ）。T115 で書いた記述の誤り。T115 のレビューで発見。実装箇所を `SpeakerDiarizationService` ＋ `TranscriptionService.TranscribeFileAsync` (catch) へ訂正し、例外が Error イベントへ変換される経路も本文に明記した。文書のみの修正でソース変更なし (2026-08-22)
- [x] **T115** 話者識別（Speaker Diarization）を sherpa-onnx で導入した。**ファイル文字起こし経路のみ**（sherpa-onnx のオフライン Diarization は音声全体を 1 配列で要求するためライブ経路には載らない）。既定は無効で、`settings.json` で有効化する。出力は `[時刻 - 時刻] [ファイル] [話者N] テキスト`。話者の割り当ては開始時刻ではなく**重複時間**で決め、同点は小さい話者 ID、重複ゼロは `[話者不明]`（直前の話者を引き継がない）。ADR-0001 に従いインターフェース抽象は作らず、sherpa-onnx 依存を `SpeakerDiarizationService` 1 クラスに閉じ、マージ規則は `internal static` の純粋関数にしてテスト 38 件で固定した。**調査で判明した地雷:** モデル未配置時に sherpa-onnx は NULL ハンドルを返すが C# ラッパーが検査せず、使うとアクセス違反で**プロセスが即死する**（catch 不能）ため、生成前の `File.Exists` 検査を必須にした (2026-08-22) → [ADR-0003](../adr/0003-speaker-diarization-with-sherpa-onnx.md) / [詳細](./T115-speaker-diarization.md)
- [x] **T109** `README.md` の「ビルド」節が存在しない `AudioCaptureApp.sln` を参照していた（正: `AudioCaptureApp.slnx`）。T108 の作業中に発見。当該 1 行を `.slnx` へ修正（`docs/archive/` 内の同種の記述は参照専用のため対象外） (2026-08-22)
- [x] **T133** ハーネスが `docs/spec/` 以外の場所に仕様文書が作られることを禁じていない。実際に `docs/superpowers/specs/2026-08-19-silence-cut-before-whisper-design.md`（T112 の設計文書）が生まれ、内容は `docs/tasks/T112-silence-cut-before-whisper.md` と重複したうえ T125 / T129 の改訂を取り込んでいない古い記述になっていた。50-spec-standards §1 と 00-ways-of-working 法 2 に禁止規則を明記し、当該ファイルを削除した。CLAUDE.md の「ドキュメントの置き場」表にも同じ禁止を明記 (2026-08-22)
- [x] **T131** `docs/spec/04_sequence_diagram.md` のライブ文字起こしの図が「1,000 行超は先頭から破棄」と書いているが、T130 で上限は 100 行になっており `REQ-LIVEVIEW-04` と食い違う（T130 が 01_requirements.md しか直さなかった）。T129 の作業中に発見。図を 100 行へ修正 (2026-08-21)
- [x] **T132** `docs/spec/01_requirements.md` の §8 で `REQ-TRX-LIVE-06` が 2 行に重複して振られている（`_sessionClock` の要件と、`REQ-TRX-LIVE-11` と内容が重なる「セッション終了処理は最大 30 秒待機」の要件）。ID は再利用しない規約（50-spec-standards §3）に反する。後者は REQ-TRX-LIVE-11 に吸収して行ごと消すのが妥当。T129 の作業中に発見。重複していた後者の行を削除し REQ-TRX-LIVE-11 に一本化 (2026-08-21)
- [x] **T129** ライブ文字起こしの出力粒度が 20 秒固定で、「発話が終わった」契機が存在しなかった。マイクは無音でも WASAPI が供給を続けるためギャップ分割（500ms）が発火せず、`StaleBufferAge`（20 秒）も `BufferThresholdSamples`（20 秒）と同値のため連続供給では常に空振りしていた。確定契機に「末尾に `MergeGapSeconds`（2.0 秒）以上の無音が積まれたら確定する」を追加し（REQ-TRX-LIVE-13）、滞留契機を「バッファ先頭サンプルの滞留」から「供給の途絶 5 秒」へ定義し直した（REQ-TRX-LIVE-12、定数も `StaleSupplyIdle` へ改名）。保持時間を `MergeGapSeconds` と同値にしたため Whisper の呼び出し回数は据え置き。実測: 発話終了から表示まで 0〜20 秒 → 3〜4 秒以内、1 発話が複数行に割れないことも確認。T114 の実機確認で発覚 (2026-08-21) → [詳細](./T129-live-transcript-endpointing.md)
- [x] **T130** 文字起こし表示ウィンドウの改良（初期サイズ 480x240 / ボタン文言「文字起こし表示」→「表示」/ 録音開始成功時に表示内容をクリア / 表示上限 1,000 行 → 100 行）。T114 §8-1 の未解決事項「表示をクリアする手段が無い」はこれで解決 (2026-08-21) → [詳細](./T130-live-transcript-window-tweaks.md)
- [x] **T128** 文字起こし表示ウィンドウで `ScrollIntoView` を `CollectionChanged` ハンドラー内から同期的に呼んでおり、`ItemContainerGenerator` の状態と食い違って `InvalidOperationException`（「ItemsControl が項目のソースと一致していません」）でプロセスが落ちる。1 チャンクから複数行が連続で届くと再現。`Dispatcher.BeginInvoke(Background)` へ後回しにして解消（実測: 修正前 3/3 クラッシュ → 修正後 0/3）。T114 の実機確認で発覚 (2026-08-21) → [詳細](./T128-live-transcript-scroll-crash.md)
- [x] **T114** リアルタイム文字起こしのテキストを表示するサブウィンドウを追加する（320x240・リサイズ可・9pt・録音停止で閉じない・プロセス終了で閉じる）。`SegmentTranscribed` を初めて購読した (2026-08-20) → [詳細](./T114-live-transcript-window.md)
- [x] **T113** 音声ファイル文字起こしの前にオプション指定モーダルダイアログを表示する（ファイル名表示・開始時刻 `hh:mm` 指定・進捗表示・キャンセル）。補助ウィンドウの追加にあたり [ADR-0002](../adr/0002-secondary-windows-share-mainviewmodel.md) を起票 (2026-08-20) → [詳細](./T113-file-transcription-options-dialog.md)
- [x] **T125** 短い物音が結合幅（既定 2.0 秒）内に複数あると 1 区間にまとまり、大半が無音のまま Whisper に渡る（例: 0.1 秒のクリック音 2 つが 1.5 秒の区間になる）。足切りを「結合後の区間幅」から「0.2 秒以上続く有声ランを含むか」へ変更した（有声密度案は実体のある短い発話を巻き添えで捨てるため不採用）。T112 のレビューで発見 (2026-08-20) → [詳細](./T125-voiced-region-density-cutoff.md)
- [x] **T126** `ProcessChunk` の `results` が `try` の内側で宣言されているため、Whisper がキャンセル例外を投げると**完了済み区間の行まで破棄される**。それらは `SegmentTranscribed` で画面には既に出ているので、`.txt` と画面が食い違う。T112 以前からある問題だが、1 チャンクが複数区間に分かれるようになり影響範囲が広がった。T112 のレビューで発見 (2026-08-20) → [詳細](./T126-processchunk-results-scope.md)
- [x] **T127** 録音停止の最悪レイテンシが T112 で悪化した。1 チャンクが最大 10 区間に分かれうるため、停止時の推論回数が最悪 3 回 → 30 回になり、`StopGraceTimeout`（30 秒）を超えると `DisposeProcessorSafely(workerExited: false)` の意図的リーク経路に入る（T117 が直した症状の再来）。`ProcessChunk` の区間ループは `token` しか見ておらず、猶予期間中はまだキャンセルされていない。`_isRunning` を見る案は排出処理（drain）を握り潰すので不可 — `interruptible` 引数で経路を分ける必要がある。T112 のレビューで発見 (2026-08-20) → [詳細](./T127-stop-latency-region-loop.md)
- [x] **T112** 無音区間を Whisper に渡さないようにカットし、無音部でのハルシネーション（「ご視聴ありがとうございました」等）を抑止する (2026-08-20) → [詳細](./T112-silence-cut-before-whisper.md)
- [x] **T124** ブランチ運用（develop 最新化 → feature ブランチ作成 → ゲート全緑後に commit/push/PR）を規範化し、保護ブランチ上のソース編集をフックで deny する (2026-08-18) → [詳細](./T124-branch-workflow-enforcement.md)
- [x] **T123** GPU が使えない環境でも `GpuAvailable` が真になり「GPU 実行」と表示される（GPU 版ランタイム DLL はデバイスが無くても読み込めるため。ネイティブログの `backends` 数と重みの配置先で判定するよう変更） (2026-08-18) → [詳細](./T123-gpu-availability-false-positive.md)
- [x] **T122** `TranscriptionService.LoadModel` がモデル読み込みの失敗を返せない（`WhisperFactory.FromPath` はネイティブ側の読み込み失敗を例外にせずファクトリを返すため、壊れたモデルでも「読み込み完了」と表示されていた。`CreateBuilder()` で失敗を確定させる） (2026-08-18) → [詳細](./T122-loadmodel-failure-not-reported.md)
- [x] **T119** 「文字起こしにGPUを使用する」を OFF にしても GPU で動き続ける（`RuntimeLibraryOrder` による CPU 限定の再読み込みは、Whisper.net がネイティブランタイムをプロセス単位で 1 度しかロードしないため空振りしていた。`WhisperFactoryOptions.UseGpu` へ変更） (2026-08-18) → [詳細](./T119-gpu-toggle-ineffective.md)
- [x] **T121** 録音中・停止処理中・ファイル文字起こし中は「保存先を開く」を無効化する（直近の成果物が現在の作業と食い違い、表示上も違和感がある。T111 の追補） (2026-08-18) → [詳細](./T121-disable-open-folder-while-busy.md)
- [x] **T111** 文字起こし完了時に保存先フォルダを開けるようにする（要件 3） (2026-08-16) → [詳細](./T111-open-result-folder.md)
- [x] **T118** マイクのモニタリング開始失敗でアプリが起動できない（`E_HANDLE` が `MainViewModel` コンストラクターから漏れる） (2026-08-16) → [詳細](./T118-mic-monitor-startup-crash.md)
- [x] **T120** 供給が止まったバッファを時間経過で書き出す（ミュート中に書き出しが無制限に遅延し、`.txt` の行順が時刻順にならない） (2026-08-16) → [詳細](./T120-stale-buffer-flush.md)
- [x] **T117** 録音停止時に長時間ブロックし `Cannot dispose while processing` で異常終了する (2026-08-16) → [詳細](./T117-stop-session-crash.md)
- [x] **T116** ライブ文字起こしの記録時刻が実時刻とずれる（無音／ミュート区間で時計が進まない）＋短い・小音量の発話が丸ごと捨てられる (2026-08-16) → [詳細](./T116-live-transcript-timestamp.md)
- [x] **T110** スピーカー（ループバック）のレベルメーターを録音停止中でも動作させる (2026-08-16) → [詳細](./T110-speaker-meter-always-on.md)

- [x] **T108** 実行基盤を .NET 8 から .NET 10 へ引き上げる（アプリ／テスト／CI） (2026-08-15) → [詳細](./T108-net10-upgrade.md)
- [x] **T107** `SettingsService` のフィールド初期化をコンストラクターに集約（宣言時とコンストラクターへの分散を解消） (2026-08-15)

- [x] **T106** `.claude/settings.local.json` を git 管理から外す (2026-08-14) → [詳細](./T106-untrack-local-settings.md)
- [x] **T105** 軽微な静的解析指摘の解消（CA1822 / CA1825） (2026-08-14) → [詳細](./T105-minor-analyzer-findings.md)
- [x] **T104** 非同期の是正 — 同期呼び出しの解消と `CancellationToken` の伝播（CA1849 / CA2016） (2026-08-14) → [詳細](./T104-async-discipline.md)
- [x] **T103** `catch (Exception)` の局所化と理由付与（CA1031） (2026-08-14) → [詳細](./T103-narrow-exception-handling.md)
- [x] **T102** カルチャー・文字列比較の明示（CA1305 / CA1307） (2026-08-14) → [詳細](./T102-culture-and-string-comparison.md)
- [x] **T101** Dispose パターンの是正（CA1063 / CA1816 / CA1001 / CA2213 / CA2000） (2026-08-14) → [詳細](./T101-dispose-pattern-cleanup.md)
- [x] **T100** 開発ハーネスの整備（`docs/harness/` の規範・強制フック・ビルドゲート） (2026-08-14) → [詳細](./T100-dev-harness.md)
